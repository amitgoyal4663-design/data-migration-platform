# The application writes one JSON object per line, so this only has to say so — and name the
# timestamp field, which ECS calls @timestamp rather than the "time" Fluent Bit assumes.
[PARSER]
    Name        json
    Format      json
    Time_Key    @timestamp
    Time_Format %Y-%m-%dT%H:%M:%S.%LZ
    Time_Keep   On
